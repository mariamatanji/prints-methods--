# prints-methods--
Read two strings and two integers from input and call printEarnings() twice: once with the first and third inputs as arguments, and again with the second and fourth inputs as arguments.
